@if (User.Identity.IsAuthenticated)
{
    using (Html.BeginForm("SignOut", "Account", new { area = "Common" }, FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user" style="margin-right: 10px;"></i>@User.Identity.Name <b class="caret"></b></a>
        <ul class="dropdown-menu">
            @*<li>
                    <a href="#"><i class="fa fa-fw fa-user"></i> Profile</a>
                </li>
                <li>
                    <a href="#"><i class="fa fa-fw fa-envelope"></i> Inbox</a>
                </li>
                <li>
                    <a href="#"><i class="fa fa-fw fa-gear"></i> Settings</a>
                </li>
                <li class="divider"></li>*@
            <li>
                <a href="javascript:document.getElementById('logoutForm').submit()">
                    <i class="fa fa-fw fa-power-off"></i>&nbsp;&nbsp;&nbsp; Đăng xuất
                </a>
            </li>
        </ul>
    }
}
